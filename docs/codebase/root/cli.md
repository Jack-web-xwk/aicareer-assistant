# 概要：`cli.py`（仓库根目录）

**对应源码**：[../../../cli.py](../../../cli.py)

## 路径与角色

交互式或参数化 CLI：启动模式选择、调用子命令（与 `start-services` 或本地脚本配合，以实现为准）。

## 对外接口

`python cli.py` 或带 `-Mode` 等参数。

## 关键依赖

标准库/typer/click（以文件为准）。

## 数据流 / 副作用

可能 spawn 子进程启动前后端。

## 与前端/其它模块的衔接

开发者本地工作流。

## 注意点

Windows 控制台编码与路径分隔符。
