# AI Career Assistant - Backend Dependencies
# Python 3.11+ Required

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Database
sqlalchemy>=2.0.35
aiosqlite>=0.20.0
asyncpg>=0.30.0          # PostgreSQL async driver
redis>=5.0.0             # Redis client
alembic>=1.14.0          # Database migrations

# AI & LLM (Multi-Provider Support)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0        # OpenAI + OpenAI-compatible APIs (DeepSeek, Zhipu, Ollama, Qwen)
langgraph>=0.2.27
openai>=1.50.0

# Note: All supported LLM providers use OpenAI-compatible API format:
# - OpenAI: Native support
# - DeepSeek: https://api.deepseek.com (compatible)
# - Zhipu GLM: https://open.bigmodel.cn/api/paas/v4 (compatible)
# - Ollama: http://localhost:11434/v1 (compatible)
# - Anthropic: via langchain-openai with custom headers
# - Qwen (DashScope): https://dashscope.aliyuncs.com/compatible-mode/v1 (compatible)

# Document Parsing
pdfplumber>=0.11.0
python-docx>=1.1.0

# Web Scraping
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Optional third-layer fetch after Jina: pip install "scrapling[fetchers]"

# Audio Processing
pydub>=0.25.0

# Configuration
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Auth
python-jose[cryptography]>=3.3.0    # JWT tokens
passlib[bcrypt]>=1.7.4              # Password hashing
bcrypt>=4.0.0,<5.0.0               # bcrypt 5.x 与 passlib 不兼容

# Utilities
httpx>=0.27.0
aiofiles>=24.1.0
markdown>=3.7

# Testing (optional)
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
